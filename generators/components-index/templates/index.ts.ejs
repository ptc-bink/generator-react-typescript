/** 
 * This is automatically generated components index file. 
 * */
<% components.forEach(function(component){ %>
import <%= component %> from './<%= component %>/index';<% }); %>
 
export { 
        <%= components.join(', \r\n\t\t') %> 
    }